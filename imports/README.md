# imports

eXtreme Gammonで解析済みの `.xg` または `.xgp` ファイルを、このフォルダまたは配下のサブフォルダへ追加してください。

`imports/` 以下は再帰的に読み込まれるため、整理用のサブフォルダを何階層作っても構いません。

ファイルを追加して `main` ブランチへコミットすると、GitHub Actionsが自動的にデータベースを再構築します。
